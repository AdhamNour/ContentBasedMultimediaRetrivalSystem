import 'package:flutter/material.dart';

class VideoRetrivalComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('this is video retrival component'),
    );
  }
}